export default {
  routes: [
    // ========================================PC管理系统========================================
    // { path: '/', redirect: '/login' },
    { path: '/', component: require('./views/index.vue') }
    // { path: '*', component: require('_views/404.vue') }
  ]
}